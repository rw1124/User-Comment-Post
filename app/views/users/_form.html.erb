<%= form_for(user) do |f| %>
  <fieldset>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

  <%= f.label :first_name %>
  <%= f.text_field :first_name, size: "100x6" %>

  <%= f.label :last_name %>
  <%= f.text_field :last_name, size: "100x6" %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :password %>
  <%= f.text_field :password %>


  <div class="actions">
    <%= f.submit %>
  </div>
  </fieldset>
<% end %>
